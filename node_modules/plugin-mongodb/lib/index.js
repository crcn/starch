var mongoose = require("mongoose"),
getConnectionString = require("./getConnectionString");
exports.plugin = function(loader) {
	return mongoose.createConnection(getConnectionString(loader.params("mongodb")));
}
